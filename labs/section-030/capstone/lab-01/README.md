# section-030 / capstone: New Toolchain, New Host

QEMU VM for the LFCS course — the Section 030 capstone. Compile and install a reporting tool from source to an exact path with a feature disabled, then define and start a persistent libvirt domain for it to report on, in a single integrated maintenance window.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS002.git -c labs/section-030/capstone/lab-01
```
