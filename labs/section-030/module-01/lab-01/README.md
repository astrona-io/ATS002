# section-030 / module-01: Compile & Install From Source

QEMU VM for the LFCS course — extracting a source tarball, discovering `configure` flags, and building/installing a binary to an exact path with a feature disabled.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS002.git -c labs/section-030/module-01/lab-01
```
