# section-030 / module-02: libvirt Virtual Machine Lifecycle

QEMU VM for the LFCS course — defining a persistent KVM domain around an existing qcow2 disk image, configuring autostart, and managing graceful vs. hard shutdown.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS002.git -c labs/section-030/module-02/lab-01
```
