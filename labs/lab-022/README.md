# lab-022: Docker Container Lifecycle

QEMU VM for the LFCS course — stopping a running container, extracting precise facts out of another container's metadata with `docker inspect --format`, and launching a new detached container with a memory limit and a port mapping.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS002.git -c labs/lab-022
```
