# section-010 / module-03: Kernel Modules — Loading, Parameters, and Blacklisting

QEMU VM for the LFCS course — loading the `dummy` module with a persistent parameter, and blacklisting `pcspkr` so it never auto-loads again.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS002.git -c labs/section-010/module-03/lab-01
```
