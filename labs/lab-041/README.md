# lab-041: AppArmor Profile Path Denial

QEMU VM for the LFCS course — diagnosing an AppArmor path-based denial via the kernel audit trail and repairing the profile without leaving it in complain mode.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS002.git -c labs/lab-041
```
