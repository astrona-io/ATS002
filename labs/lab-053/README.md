# lab-053: APT Basic Package Operations

QEMU VM for the LFCS course — running a realistic maintenance pass: refresh the index, preview and apply upgrades, install a new package, then fully purge an unneeded one including its configuration and orphaned dependencies.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS002.git -c labs/lab-053
```
