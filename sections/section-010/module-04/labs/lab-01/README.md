# section-010 / module-04: udev Device Rules — Stable Names for Removable Storage

QEMU VM for the LFCS course — writing a custom udev rule that gives an attached disk a stable, serial-matched symlink instead of relying on its unstable kernel-assigned device letter.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS002.git -c sections/section-010/module-04/labs/lab-01
```
