# section-050 / module-02: dpkg Low-Level Package Management

QEMU VM for the LFCS course — inspecting a standalone `.deb` before installing it, installing it directly with `dpkg -i`, answering file-ownership questions in both directions, and recovering a real, unrelated package left in a half-configured state.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS002.git -c sections/section-050/module-02/labs/lab-01
```
