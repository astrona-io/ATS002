# section-050 / module-05: APT Package Groups & Bulk Operations

QEMU VM for the LFCS course — installing a build toolchain as one atomic `apt install` transaction, finding an entire installed package family by naming pattern, and bulk-holding that whole family together ahead of a risky upgrade.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS002.git -c sections/section-050/module-05/labs/lab-01
```
